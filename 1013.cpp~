#include <bits/stdc++.h>

using namespace std;

int main()
{
	long int a, b, c, maiorAB, maiorABC;
	cin >> a >> b >> c;
	maiorAB=(a+b+abs(a-b))/2;
	maiorABC = (maiorAB+c+abs(maiorAB-c))/2;
	printf("%ld eh o maior\n", maiorABC);
}
