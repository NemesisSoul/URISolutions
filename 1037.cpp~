#include <bits/stdc++.h>

using namespace std;

int main(){
	double input;
	cin >> input;
	if(input >= 0.00000 && input <= 25.00000)
		printf("Intervalo [0,25]\n");
	else if(input > 25.00000 && input <= 50.00000)
		printf("Intervalo (25,50]\n");
	else if(input > 50.00000 && input <= 75.00000)
		printf("Intervalo (50,75]\n");
	else if(input > 75.00000 && input <= 100.0000)
		printf("Intervalo (75,100]\n");
	else
		printf("Fora de intervalo\n");
	
	return 0;
}
